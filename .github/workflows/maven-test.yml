name: maven-test.yml
on:
  push:
    branches:
      - master
      - main
      - 'releases/*'
      - 'feature/*'
jobs:
  mvn-clean-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v3
      - name: mvn-clean-package
        uses: maven:3.9.8-eclipse-temurin-22
        run: |
          mvn clean test